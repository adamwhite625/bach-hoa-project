package com.example.frontend2.ui.main; // <-- Đặt đúng package

public enum SortType {
    DEFAULT,            // Mặc định (theo thứ tự từ API)
    PRICE_ASCENDING,    // Giá tăng dần
    PRICE_DESCENDING   // Giá giảm dần
}
