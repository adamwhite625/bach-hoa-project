{
  "name": "bach-hoa-project",
  "version": "1.0.0",
  "scripts": {
    "dev": "cd admin-web && npm install && npm run dev",
    "build": "node build.js",
    "vercel-build": "node build.js",
    "preview": "cd admin-web && npm run preview"
  },
  "dependencies": {
    "node-cron": "^4.2.1",
    "socket.io": "^4.8.1",
    "vnpay": "^2.4.4"
  }
}
